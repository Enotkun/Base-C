#include <stdio.h>

int main(void)
{
    // array of numbers
    int a[] = {0, 0, 0, 0};

    // array of numbers
    double b[] = {1.1, 2.2, 3.3, 4.4};

    // array of symbols
    char c[] = {'T', 'O', 'S', 'H'};

    // string управляющий символ \0 в оперативной памяти означает конец строки, в строках добавляется автоматически
    char d[] = {'A', 'N', 'T', 'O', 'N', '\0'};

    // string
    char g[] = "ANTON";

    return 0;
}